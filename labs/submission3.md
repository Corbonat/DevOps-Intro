# Task 1

Created branch `feature/lab3` from updated `main`.
Added `.github/workflows/github-actions-demo.yml` from the GitHub Actions quickstart.
Committed and pushed changes to trigger workflow on `push`.
Checked workflow run and logs from console using `gh` CLI.
